Download the discovery document and save it
`https://mapsbooking.googleapis.com/$discovery/rest?version=v1alpha&key=<GOOGLE API KEY></GOOGLE>`

Generate the class with:
`./bin/generate-api gen . --file=../rest.json`
